const express = require('express');
const router = express.Router();

// TODO: Add buyer routes
// Example routes:
// GET /api/buyers - Get all buyers (admin only)
// GET /api/buyers/:id - Get buyer by ID
// PUT /api/buyers/:id - Update buyer profile (requires auth, buyer role)

module.exports = router;
